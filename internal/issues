[Note added 2026-07-02] A review + implementation plan for these four items lives at
`internal/ui-humanization-plan.md` (work packages WP-A…WP-E). The lines starting "— addressed:"
below were appended by WP-E; the original feedback text above each is unchanged.

1. Need to show rationale/inference along with the prediction. Only then it means meaningful to human audience.
Need to show the prediction in a way that is easy to understand.
— addressed: predictions now carry attached plain-language rationale. The backend orchestrator emits a
  deterministic 2–3 sentence verdict() (recommendation + dominant driver + the turnover-authenticity divergence
  note) shown directly under the Health Card recommendation banner and as stage 9's headline; every KPI also
  carries a one-line inference or an ⓘ tooltip. (WP-A verdict/findings + WP-D page wiring)

2. Now tha animated UI is good, but like in a jupyter notebook, the analysis should show some plot/findings and inference. How do we make them understand that machine leaning stuff is happening behind the sceness.
Currently these are just table items:
1
Scenario Lock-in
Completed
2
Data Ingestion — Breadth Reveal
Completed
3
Data Integration
Completed
4
Feature Engineering
Completed
5
Cross-Source Synthesis
Completed
6
Peer Segmentation
Completed
7
Scoring
Completed
8
Explainability
Completed
9
Financial Health Card
Completed

One idea is to have a details link/collapse, that expands the details of what all happened under the hood. Imagine what jupyter notebook shows. But should not look like console logs. Should be meaningul to the bank ops/management level user.
language used has to be human friendly. This is meant to be a human readable UI.
— addressed: each completed stage now leaves a persistent, expandable notebook-style cell (plain-language
  headline + tone-coded findings callouts + the stage's plot/visual) instead of collapsing to a bare
  "title · Completed" row. After a run all 9 cells remain (stage 9 open); the terminal-style execution console
  is demoted to Technical-view ambience only — the record itself reads as human analysis, not console logs.
  (WP-A findings + WP-C render_findings/render_stage_cell on the Pipeline page)

3. Use of simple/technical toggle view can help here if we want to not suffocate user with high technicals once we start fixing for above issues.
— addressed: a global "View: Simple · Technical" toggle sits in the sidebar on every page (default Simple).
  Simple hides model names, SHAP, the console, engineering names and states inferences in words; Technical
  restores everything. Backend findings are tagged technical=True/False so gating is data-driven, not ad hoc.
  (WP-B state.is_technical()/page_setup toggle, applied across WP-C/WP-D)

4. Across all pages more analysis rational without cluttering UI is useful There could be info icons etc that can be used.
— addressed: kpi() gained a tip= parameter rendering an accessible ⓘ info glyph (hover + keyboard focus) with
  all tooltip copy centralized in components/glossary.py (each entry ≤25 words, jargon-free). Info icons and
  one-line inferences were added across the Dashboard, Health Card, Explainability and Pipeline pages — rationale
  without vertical clutter. (WP-B kpi tooltip + glossary, WP-D page-by-page wiring)