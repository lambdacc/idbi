"""Reusable Streamlit components for the CreditPulse demo surface.

`frontend/` renders only — every number here originates in `ml/` via
`backend/services` (module-boundary rule, implementation-plan.md §3).
"""
