"""SentinelPulse detection eval (Track 05, WP-5M) — EVAL / TESTS ONLY.

This subpackage is the ONLY place in the track allowed to read the ground-truth
labels file. Serving/feature/score modules never import it.
"""
